# Sensitive files — DO NOT commit secrets
config/opencode.jsonc
*.log
node_modules/

.idea/
.superpowers/

# Mem0 plugin build artifacts that shouldn't be tracked as editable
# (the dist/ bundle is needed for the plugin to work, so we include it)
